# fastapi==0.115.0
# uvicorn[standard]==0.30.6
# python-multipart==0.0.9
# Pillow==10.4.0
# opencv-python-headless==4.10.0.84
# # numpy==1.26.4
# requests==2.32.3

# # Optional, only needed for the 'ai_extend' mode (background removal / segmentation).
# # The app falls back to mirror_extend (still no blur, just not subject-aware)
# # if this isn't installed. Note: the code explicitly pins the u2net model via
# # new_session("u2net") -- rembg's newer default model (Bria RMBG) carries a
# # non-commercial license, which isn't appropriate for a free/open tool.
# # rembg==2.0.59
# # rembg==2.0.83   # updated
# numpy>=1.26,<3.0
# rembg>=2.0.83

# # For true generative outpainting ("ai_generate" mode), see the separate,
# # much heavier requirements-generative.txt.

fastapi==0.115.0
uvicorn==0.30.6
python-multipart==0.0.9
Pillow>=12.1.0,<13.0.0
opencv-python-headless==4.10.0.84
numpy>=1.26,<3.0
requests==2.32.3
rembg>=2.0.83

fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
Pillow==10.4.0
opencv-python-headless==4.10.0.84
numpy==1.26.4

# Optional, only needed for the 'ai_extend' mode (background removal / segmentation).
# The app falls back gracefully to blurred padding if this isn't installed.
rembg==2.0.59
